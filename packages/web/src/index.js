/*
  Main index file for aplication
*/

import React from 'react';
import ReactDOM from 'react-dom';

// Components
import BootSetup from './BootSetup';

ReactDOM.render(
    <BootSetup/>,
  document.getElementById('root')
);